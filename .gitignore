# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

bun.lockb

# CV — cílené životopisy a pracovní výstupy zůstávají mimo repozitář i mimo web.
# Veřejná PDF v public/assets/resume/ se naopak commitují: bez nich by odkaz
# hero.cvUrl po nasazení vracel 404.
cv/out/
cv/targeted/

# Nástroje agenta. Impeccable se obnoví přes `npx impeccable install`,
# settings.local.json je už názvem lokální a .impeccable/ je jen stav a cache.
.claude/
.impeccable/

# Interní produktový a portfoliový kontext. Nese obchodní čísla VečerkyPlus,
# a repozitář je veřejný — proto zůstávají mimo git, ne jen mimo web.
PRODUCT.md
CLAUDE_PORTFOLIO_CONTEXT.md
